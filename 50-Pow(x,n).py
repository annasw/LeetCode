# this is medium difficulty
# i guess maybe they wanted like a lower-level answer... but this isn't even that high-level it's just built into python

class Solution:
    def myPow(self, x: float, n: int) -> float:
        return x**n
